# Sibling repositories (each is its own git repo - this umbrella only
# carries the project README, LICENSE and this file)
/inkwash/
/inkwash-server/
/inkwash-desktop/
/inkwash-mcp/
/inkwash-firmware/

# Internal / tooling files not meant for the public umbrella
/.claude/
/.omo/
.DS_Store
# workspace-local progress/history/plan docs (never commit to the public umbrella)
/docs/
*.code-workspace